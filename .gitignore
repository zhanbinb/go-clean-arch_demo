# Build artifacts
bin/
engine
app/
*.out
coverage.out
gotestsum.json.out

# Local environment / secrets
.env
.env.local
configs/config.local.yaml

# Generated code
api/gen/
docs/swagger/docs.go
docs/swagger/swagger.json
docs/swagger/swagger.yaml

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Air hot-reload temp
tmp/

# Vendor (we use Go modules, not vendoring)
vendor/


# Defensive: stray top-level binaries (Makefile builds into bin/, but
# manual `go build` could leak here)
/rest
/grpc
/engine
